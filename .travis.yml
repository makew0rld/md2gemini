language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

cache: pip

before_script: black --check --exclude 'unitable\.py' .
script: pytest

notifications:
  email:
    on_success: never
    on_failure: always
